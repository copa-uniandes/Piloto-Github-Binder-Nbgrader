# This is a list of dependency python packages to launch the interactive code tools, Binder and Thebe. Include only packages required for code block execution in jupyter notebooks.
name: ejercicio-piloto
channels:
  - anaconda
